---
- name: Install package updates
  hosts: default

  tasks:
    - name: Update and upgrade apt packages
      become: true
      ansible.builtin.apt:
        upgrade: true
        update_cache: true
        autoremove: true
