---
- name: Install package updates
  hosts: default

  tasks:
    - name: update and upgrade apt packages
      become: yes
      apt:
        upgrade: yes
        update_cache: yes
        autoremove: yes
